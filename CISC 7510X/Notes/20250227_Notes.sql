--Postgres copy commands
  --ls was a helpful command?
  --he didn't need to unzip anything
  --doing this with ubuntu looked more fun
  --he didn't need to do any access management due to STDIN
copy split from STDIN with DELIMITER ',' NULL '' --Missed the NULL which adds handling

